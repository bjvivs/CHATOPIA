<UserControl x:Class="CHATOPIA.Views.MessageStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<ListView ItemsSource="{Binding Messages}">
			<ListView.ItemTemplate>
				<DataTemplate>
					<!-- 
						If we do not specifiy a 'Path' attribute for binding, it is assumed we want to bind the object itself.  in this case, since
						we are binding a list of strings, we are Ok to bind to the object (a string) directly.  in real world applications, you
						will typically bind to a property of the object in the list.
					-->
					<TextBlock Text="{Binding}" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
    </Grid>
</UserControl>
