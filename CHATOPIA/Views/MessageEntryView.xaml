<UserControl x:Class="CHATOPIA.Views.MessageEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		
		
		<TextBox Text="{Binding Message, Mode=TwoWay}" />
		
		<!-- If the x:Name of our control matches an action on the view model, Caliburn Micro will link the two together.  This is a convention. -->
		<Button x:Name="Send" Content="Send" Grid.Column="1" />
	</Grid>
</UserControl>
